"""Text cleaning and normalization utility function skeletons."""


def clean_text(text: str) -> str:
    """Sanitize raw text by removing unwanted characters or markup.

    Args:
        text: Raw text string.

    Returns:
        str: Sanitized text string.
    """
    # TODO: Stage 5 — Implement text cleaning and stripping logic
    raise NotImplementedError("Text cleaning will be implemented in Stage 5.")


def normalize_whitespace(text: str) -> str:
    """Normalize internal and external whitespace within string.

    Args:
        text: Input string with potentially irregular whitespace.

    Returns:
        str: String with collapsed uniform single-space whitespace.
    """
    # TODO: Stage 5 — Implement whitespace collapse logic
    raise NotImplementedError("Whitespace normalization will be implemented in Stage 5.")
